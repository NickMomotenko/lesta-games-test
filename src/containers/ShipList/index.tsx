import { ShipCard } from "../../components/ShipCard";
import { ShipListWrapp } from "./styled";

export const ShipList = () => {
  return (
    <ShipListWrapp>
      <ShipCard />
    </ShipListWrapp>
  );
};
